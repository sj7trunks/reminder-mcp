# Server Configuration (HTTP mode)
PORT=3000
HOST=0.0.0.0

# API Key for MCP endpoint authentication (REQUIRED for production)
# Generate with: openssl rand -hex 32
API_KEY=your-secret-api-key-here

# JWT Signing Secret (REQUIRED for web frontend auth)
# Generate with: openssl rand -hex 32
SECRET_KEY=your-jwt-secret-here

# Authentik SSO (optional — for forward auth auto-login)
# AUTHENTIK_HOST=https://idp.pendulus.net
# In development, Authentik auto-login is off by default.
# Set true to allow Authentik header-based auto-login in dev.
# AUTHENTIK_IN_DEV=false

# Database Configuration
# For SQLite (development)
# DATABASE_TYPE=sqlite
# DATABASE_PATH=./data/reminder.db

# For PostgreSQL with pgvector (production)
DATABASE_TYPE=postgres
DATABASE_URL=postgresql://reminder:reminder@postgres:5432/reminder_mcp
POSTGRES_USER=reminder
POSTGRES_PASSWORD=reminder

# Embedding API for vector search (OpenAI-compatible)
# Required for semantic memory search via pgvector
# EMBEDDING_API_URL=https://api.openai.com
# EMBEDDING_API_KEY=sk-...
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIMENSIONS=1536

# Redis Configuration (optional - for Bull queue)
# If not provided, scheduling will use in-memory polling
# REDIS_URL=redis://localhost:6379

# OpenAI API key (required for memory embeddings in PostgreSQL mode)
# OPENAI_API_KEY=sk-...

# Webhook Configuration (optional)
# WEBHOOK_URL=https://your-app.com/notifications

# Default timezone
DEFAULT_TIMEZONE=America/Los_Angeles

# Logging
LOG_LEVEL=info
